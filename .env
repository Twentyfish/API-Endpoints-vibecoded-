# PostgreSQL Database Connection
# Replace these values with your actual Railway PostgreSQL credentials

# Full connection string format:
DATABASE_URL=postgresql://postgres:UMlftzQXuwyQVSMqEMMiNXBXjdhwYxwj@nozomi.proxy.rlwy.net:47784/railway

# OR you can build it from individual components:
# DATABASE_URL=postgresql://[USERNAME]:[PASSWORD]@[HOST]:[PORT]/[DATABASE]

# Example with different credentials:
# DATABASE_URL=postgresql://myuser:mypassword123@containers-us-west-123.railway.app:5432/railway

# Port for Flask app (optional, defaults to 8080)
PORT=8080

# Important notes:
# 1. Use the PUBLIC hostname (*.proxy.rlwy.net or *.railway.app)
#    NOT the internal one (*.railway.internal)
# 2. Make sure there are no spaces around the = sign
# 3. Don't commit this file to git - add it to .gitignore
# 4. The app will automatically add ?sslmode=require if needed
